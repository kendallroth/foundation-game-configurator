# Set default behaviour (if devs don't have core.autocrlf set)
*      text=auto

*.js   text      eol=lf
*.ts   text      eol=lf
*.d.ts text      eol=lf

# Binary files that should not be modified
*.png  binary
*.jpg  binary